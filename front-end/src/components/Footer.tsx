import React from "react";
import { Box } from "@mui/material";

export default function Footer() {
  return (
    <Box
      sx={{
        width: "full",
        height: "545px",
        border: "solid",
        marginTop: "74px",
      }}
    ></Box>
  );
}
